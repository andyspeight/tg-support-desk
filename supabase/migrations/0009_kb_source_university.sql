-- University KB provenance (Duda blog at university.travelgenix.io, ingested via
-- Firecrawl). Applied via MCP. ALTER TYPE ADD VALUE only; not used in same tx.
alter type kb_source add value if not exists 'university';
